<%= form_for(set_current_user.active_relationships.find_by(followed_id: @user.id),
      html: { method: :delete },
      ) do |f| %>
  <div class="follow-btn">
    <%= f.submit "フォロー解除", class:"btn" %>
  </div>
<% end %>